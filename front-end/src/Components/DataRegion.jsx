
export function DataRegion({ children }) {
    return (
        <div className="flex flex-col min-h-[calc(100vh-7rem)] w-full bg-gray-200 p-2 border-2 rounded-xl mx-2">
            {children}
        </div>
    );
}
